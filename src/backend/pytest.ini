[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Show short tracebacks for faster scanning; use --tb=long for debugging
addopts = -v --tb=short
